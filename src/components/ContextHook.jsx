
/* 
    * useContext is a React hook that allows us to access data from any component,
    * without explicitly passing it down through props at any level.
    * 
    * 
    * Simply, useContext is used to manage Global data in the React App.
*/
const ContextHook = () => {
  return <div>Context Hook</div>;
};

export default ContextHook;
